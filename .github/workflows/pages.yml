name: Deploy NeurIPS Showcase to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "production/COMP7404/NeurIPS Showcase/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      # 站根只放入口页；论文检索页单独挂到子路径，其余文件不进网站
      - name: Stage site (landing + paper explorer)
        run: |
          mkdir -p _site
          cp "production/COMP7404/NeurIPS Showcase/index.html" "_site/index.html"
          cp -R "production/COMP7404/NeurIPS Showcase/neurips2025-oral-spotlight" "_site/neurips2025-oral-spotlight"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
